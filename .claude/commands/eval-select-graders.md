---
description: Assign code-based/model-based/human/production graders per metric — produces eval/graders.md.
allowed-tools: Skill
---

Prerequisite: `eval/golden_set.jsonl` must exist. If missing, run `/eval-build-golden` first.

Invoke the `grader-selector` skill.

Next: run `/eval-write-judge`.
