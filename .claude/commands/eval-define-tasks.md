---
description: Define and prioritize the task list for the eval — produces eval/tasks.md.
allowed-tools: Skill
---

Prerequisite: `eval/state.md` must exist with Mode set (and, for Mode B, ideally `eval/scan_report.md` present). If `eval/state.md` is missing, run `/eval-suite-init` first.

Invoke the `task-definition` skill.

Next: run `/eval-define-metrics`.
