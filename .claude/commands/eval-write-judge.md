---
description: Write LLM-judge prompts with concrete scale anchors for every model-based metric — produces eval/judge_prompts/.
allowed-tools: Skill
---

Prerequisite: `eval/graders.md` must exist. If missing, run `/eval-select-graders` first.

Invoke the `judge-prompt-builder` skill.

Next: run `/eval-run-baseline`.
