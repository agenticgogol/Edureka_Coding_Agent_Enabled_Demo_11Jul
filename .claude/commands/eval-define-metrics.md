---
description: Derive metrics per task from concrete good/bad examples — produces eval/metrics.md.
allowed-tools: Skill
---

Prerequisite: `eval/tasks.md` must exist. If missing, run `/eval-define-tasks` first.

Invoke the `metric-definition` skill.

Next: run `/eval-build-golden`.
