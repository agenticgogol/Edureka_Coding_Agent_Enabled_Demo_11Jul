# Failure backlog — ranked by frequency x severity x fixability

| Rank | Code | Count | Severity | Fixability | Score | Notes |
|---|---|---|---|---|---|---|
| 1 | stale-root-cause-reuse | 6 | high (3) | 4 | 72 | Most fixable of the three: the agent already reads the correct file before asserting the root cause — it just doesn't check whether the described bug pattern (`_wrap_16bit`, `INT16_MIN`/`MAX`) is actually still present. A cheap, tractable fix: require `finish_analysis` to quote the exact matching code snippet from a file it read, or add a lightweight code-level check that rejects a "root cause" whose cited symptom isn't literally in the file. Also the most frequent failure mode in this dataset. |
| 2 | fabricated-root-cause-unsafe-patch | 4 | critical (4) | 3 | 48 | Highest per-instance severity (a patch that would break login for every user if approved) but only moderately fixable — the underlying issue is the model asserting confident diagnoses under genuine uncertainty (no bug actually exists), which is a harder reasoning gap than #1's stale-fact problem. Partial mitigations are tractable though: a stronger prompt affordance for "no code bug found, recommend escalation" as a first-class outcome even mid-classification, plus a code-level patch-safety check that flags any diff removing a function argument from an existing call site. |
| 3 | inert-placeholder-patch | 3 | medium (2) | 3 | 18 | Moderately fixable — the common thread is a drafted patch introducing new code that never calls the actually-relevant existing module/function. A code-level check requiring the patch's added logic to reference (import/call) the module the root_cause explanation names would catch all 3 members here. Lowest score mainly because count and severity are both lower than the other two, not because it's hard to fix. |

Fixability scores are estimates — flag if any of these materially change your prioritization; scores 4 and above assume a prompt/tool-level fix without retraining, scores 2-3 assume the fix still needs a code-level guard, not just a prompt change.
